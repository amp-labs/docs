---
title: Using Ampersand AI SDK with Vercel's @aisdk
---

[AI SDK](https://ai-sdk.dev/) by Vercel — AI SDK unifies LLM access in TypeScript & makes it a real breeze to build chat-based agents. 

This is a simple example of how to use the AI SDK tools by Ampersand in a Vercel @aisdk agent.

```typescript
import { openai } from '@ai-sdk/openai';
import { generateObject, generateText } from 'ai';
import { z } from 'zod';
import { createRecordTool, updateRecordTool } from "@amp-labs/ai/aisdk";

// Vercel Agent with AI SDK tools by Ampersand
async function handleCustomerQuery(query: string) {
  const model = openai('gpt-4o');

  const { text: response } = await generateText({
    model:
      classification.complexity === 'simple'
        ? openai('gpt-4o-mini')
        : openai('o3-mini'),
    system: '... your system prompt here',
    prompt: query,
    tools: {
      // AI SDK tools by Ampersand being used here
      createRecordTool,
      updateRecordTool,
    },
  });

  return { response };
}

```
