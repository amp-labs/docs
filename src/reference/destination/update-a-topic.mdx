---
openapi: 'platform patch /projects/{projectIdOrName}/topics/{topicId}'
description: "Rename or update the metadata of an existing notification topic without changing the routes that forward its events to destinations."
---

