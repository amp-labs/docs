---
openapi: 'platform delete /projects/{projectIdOrName}/topics/{topicId}'
description: "Permanently delete a notification topic. All routes from the topic to destinations are removed and further events cannot be published to the topic."
---

