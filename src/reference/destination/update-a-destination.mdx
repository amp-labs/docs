---
openapi: 'platform patch /projects/{projectIdOrName}/destinations/{destination}'
description: "Update an existing destination's URL, credentials, or metadata. Useful for rotating webhook secrets or repointing to a new S3 bucket without downtime."
---

