---
openapi: 'platform get /projects/{projectIdOrName}/topic-destination-routes'
description: "List the routes between notification topics and destinations in a project so you can see which events are forwarded to which webhooks or buckets."
---

