---
openapi: 'platform delete /projects/{projectIdOrName}/provider-apps/{providerAppId}'
description: "Delete a provider app so it can no longer be used to authenticate new customer connections. Existing connections keep working until manually revoked."
---

