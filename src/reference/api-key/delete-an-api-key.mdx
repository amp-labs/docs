---
openapi: 'platform delete /projects/{projectIdOrName}/api-keys/{apiKey}'
description: "Permanently delete an existing API key from a project so that requests using that key are immediately rejected. This action cannot be undone."
---

