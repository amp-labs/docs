---
openapi: 'platform get /projects/{projectIdOrName}/api-keys/{apiKey}'
description: "Retrieve metadata for a single API key by its identifier, including its label, creation date, and the project it belongs to. Secret value is not returned."
---

