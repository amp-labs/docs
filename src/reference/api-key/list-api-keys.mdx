---
openapi: 'platform get /projects/{projectIdOrName}/api-keys'
description: "List all API keys that belong to a given Ampersand project so you can audit, rotate, or revoke credentials used to call the platform API."
---

