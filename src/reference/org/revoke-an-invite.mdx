---
openapi: 'platform delete /orgs/{orgId}/invites/{inviteId}'
description: "Cancel a pending invite to an organization so the invited email address can no longer accept it and join the org through that invitation link."
---

